// Billing types — gateway-agnostic
// (archivo en lib/lemonsqueezy/ por compatibilidad con imports existentes)

export type BillingInterval = "monthly" | "annual";
export type PlanKey = "starter" | "pro" | "zenmode";
